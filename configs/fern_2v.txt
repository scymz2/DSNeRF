expname = fern_2v
basedir = ./logs/release
datadir = ./data/split_allview_npy/fern_2view
dataset_type = colmap_llff   # 数据集类型，这里指使用COLMAP软件处理的LLFF(Looking at Large Forward Facing scenes)格式数据。
factor = 4                   # 下采样因子，可能用于减少原始数据的分辨率或密度。
llffhold = 8                 # LLFF数据集的holdout参数，指定每隔多少个图像用作验证而非训练。
N_rand = 4096                # 随机样本数量，用于每次迭代的训练。
N_samples = 64               # 每个光线上的采样点数，用于渲染和训练。
N_importance = 128           # 重要性采样的样本数量，用于在神经渲染中更精确地模拟细节。
use_viewdirs = True          # 是否使用观察方向作为网络的输入，用于改进渲染质量。
raw_noise_std = 1e0          # 原始噪声标准差，可能用于正则化或避免过拟合。
no_ndc = True                # 是否不使用归一化设备坐标(NDC)，与图像的几何投影有关。
colmap_depth = True          # 是否使用由COLMAP估计的深度信息，用于深度监督或改进渲染。
depth_loss = True            # 是否启用深度损失，用于训练时优化深度预测的准确性。
depth_lambda = 0.1           # 深度损失的权重，调整深度误差对总训练损失的贡献。
i_testset = 5000             # 在5000次迭代时评估测试集的性能。
i_video = 10000              # 在10000次迭代时生成视频结果。
N_iters = 50000              # 总迭代次数，指定训练过程的持续长度。